datamodule:
  _target_: 'classy.data.data_modules.ClassyDataModule'
#  task: null  # todo should be set via kwargs
#  dataset_path: null  # todo should be set via kwargs
  task: "token"
  dataset_path: "data/conll2003"
  train_dataset:
    _target_: 'classy.data.dataset.hf.HFTokenDataset.from_file'
    transformer_model: ${transformer_model}
    min_length: 5
    max_length: 500
    tokens_per_batch: 1000
    max_batch_size: 10
    section_size: 10000
    prebatch: True
    shuffle: True
  validation_split_size: 0.1
  test_split_size: 0.1
  max_nontrain_split_size: 500